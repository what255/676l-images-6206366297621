## Downloaded images

Source page:

`https://676l.com/sc/zhanghao/6206366297621/0.18`

Contents:

- `images/wallet/`: wallet icon images used by the page
- `images/teach/`: manual payment tutorial images referenced by the page script

Notes:

- The page contains repeated references to some wallet icons; only unique image files were downloaded.
- The page also contains inline SVG markup, but those are embedded directly in the HTML rather than served as standalone image files.
